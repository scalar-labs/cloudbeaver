name: Cleanup

on:
  pull_request:
    types: [closed]
  push:
    branches:
      - devel

jobs:
  delete-caches:
    name: Cleanup
    uses: scalar-labs/dbeaver-common/.github/workflows/cleanup-caches.yml@main
    secrets: inherit
